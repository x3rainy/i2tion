<h1>Bookings#index</h1>

<ul>
  <% @bookings.sort.each do |booking| %>
  <li>
    <%= booking.id %> <%= booking.location %> ---> <%= booking.start_datetime %>
    <h4><%= link_to "Edit this booking", edit_tutor_booking_path(booking.tutor, booking.id) %></h4>
    <h5><%= link_to "🔥", tutor_booking_path(booking.tutor, booking.id), method: :delete, class: "btn btn-danger" %></h5>
  </li>
  <% end %>
</ul>

